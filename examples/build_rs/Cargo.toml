[package]
name = "build_rs_example"
version = "0.1.0"
edition = "2021"
publish = false

# This is a standalone example, not part of the workspace
[workspace]

[dependencies]

[build-dependencies]
baker = { path = "../.." }
serde_json = "1.0"
